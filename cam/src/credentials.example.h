#pragma once
// =============================================================================
// cam/src/credentials.example.h — WiFi credential template.
//
// Copy this file to credentials.h and fill in your values.
// credentials.h is gitignored and must never be committed.
// =============================================================================

#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-network-password"
